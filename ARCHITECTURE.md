# 自主學習計畫申請系統 - 架構圖

## 系統總覽

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           自主學習計畫申請系統                                │
│                    FHSH Self-Learning Application System                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              Docker Container                                │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │    Frontend     │    │     Backend     │    │    MongoDB      │         │
│  │  (Nginx:3000)   │───▶│  (FastAPI:8000) │───▶│    (27017)      │         │
│  │                 │    │                 │    │                 │         │
│  │  React 19 +     │    │  Python 3.11 +  │    │  mongo:7.0      │         │
│  │  TypeScript +   │    │  Beanie ODM     │    │                 │         │
│  │  Tailwind CSS   │    │                 │    │                 │         │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│         :3000                  :8000                  :27017                │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 前端架構 (React + TypeScript)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Frontend Structure                              │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────┐
                              │   App.tsx   │
                              │  (主應用)    │
                              └──────┬──────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│  LoginPage.tsx  │       │ApplicationForm  │       │  HistoryPage    │
│                 │       │   Page.tsx      │       │     .tsx        │
│  ・學生登入      │       │                 │       │                 │
│  ・教師登入      │       │  ・計畫填寫      │       │  ・申請列表     │
│                 │       │  ・組員管理      │       │  ・狀態篩選     │
│                 │       │  ・簽章上傳      │       │  ・審核功能     │
└─────────────────┘       └─────────────────┘       └─────────────────┘
                                   │                         │
                                   ▼                         ▼
                          ┌─────────────────┐       ┌─────────────────┐
                          │ SignaturePad    │       │ApplicationDetail│
                          │    .tsx         │       │   Modal.tsx     │
                          │                 │       │                 │
                          │  ・手寫簽名      │       │  ・詳情查看     │
                          │  ・彈出視窗      │       │  ・PDF匯出      │
                          │  ・清除重簽      │       │                 │
                          └─────────────────┘       └─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           UI Component Library                               │
│                            components/ui/GlassUI.tsx                         │
├─────────────────┬─────────────────┬─────────────────┬─────────────────┬─────┤
│  GlassButton    │  GlassInput     │  GlassCard      │  GlassModal     │ ... │
│  GlassCheckbox  │  GlassTextarea  │  GlassSection   │  GlassAlert     │     │
│  GlassRadio     │  GlassSelect    │  GlassBadge     │  GlassBackground│     │
└─────────────────┴─────────────────┴─────────────────┴─────────────────┴─────┘
```

## 後端架構 (FastAPI + Python)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Backend Structure                               │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────┐
                              │   main.py   │
                              │  (FastAPI)  │
                              └──────┬──────┘
                                     │
    ┌────────────────────────────────┼────────────────────────────────┐
    │                                │                                │
    ▼                                ▼                                ▼
┌────────────┐                ┌────────────┐                ┌────────────┐
│   Routes   │                │  Services  │                │   Models   │
├────────────┤                ├────────────┤                ├────────────┤
│            │                │            │                │            │
│ auth.py    │───────────────▶│user_service│───────────────▶│  user.py   │
│ ・登入     │                │  .py       │                │  ・User    │
│ ・註冊     │                │            │                │  ・UserRole│
│ ・改密碼   │                │            │                │            │
│            │                │            │                │            │
│applications│───────────────▶│application │───────────────▶│application │
│ .py        │                │ _service   │                │  .py       │
│ ・CRUD     │                │  .py       │                │ ・Application
│ ・審核     │                │            │                │ ・Member   │
│ ・PDF匯出  │                │            │                │ ・PlanItem │
│            │                │            │                │            │
│students.py │───────────────▶│student_    │───────────────▶│student.py  │
│ ・搜尋     │                │ service.py │                │ ・Student  │
│ ・查詢     │                │            │                │            │
└────────────┘                └────────────┘                └────────────┘
                                     │
                                     ▼
                              ┌────────────┐
                              │  Database  │
                              │ mongodb.py │
                              │(Beanie ODM)│
                              └────────────┘
```

## API 端點結構

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API Endpoints                                   │
└─────────────────────────────────────────────────────────────────────────────┘

 /auth
 ├── POST   /login              使用者登入 (JWT Token)
 ├── POST   /register           使用者註冊 (開發用)
 └── POST   /change-password    更改密碼

 /applications
 ├── POST   /                   建立申請表
 ├── GET    /                   取得申請列表
 ├── GET    /{id}               取得單一申請
 ├── PUT    /{id}               更新申請表
 ├── DELETE /{id}               刪除申請表
 ├── PATCH  /{id}/review        審核申請 (教師)
 └── GET    /{id}/export-pdf    匯出PDF

 /students
 ├── GET    /                   取得學生列表
 ├── GET    /search             搜尋學生
 ├── GET    /{student_id}       取得學生詳情
 └── GET    /class/{class_name} 依班級查詢

 /
 ├── GET    /                   API根路徑
 └── GET    /health             健康檢查
```

## 資料庫結構 (MongoDB)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Database: self_learning_system                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  Collection: users                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  {                                                                           │
│    _id: ObjectId,                                                           │
│    username: String (unique),        // 帳號 (email格式)                    │
│    hashed_password: String,          // 加密密碼                            │
│    role: "student" | "teacher",      // 角色                                │
│    is_active: Boolean,                                                       │
│    // 學生專屬欄位                                                           │
│    student_id: String,               // 學號                                │
│    student_name: String,             // 姓名                                │
│    class_name: String,               // 班級                                │
│    seat_number: Number,              // 座號                                │
│    // 教師專屬欄位                                                           │
│    teacher_name: String,             // 教師姓名                            │
│    teacher_title: String,            // 職稱                                │
│    email: String,                                                            │
│    created_at: DateTime,                                                     │
│    updated_at: DateTime                                                      │
│  }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  Collection: applications                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  {                                                                           │
│    _id: ObjectId,                                                           │
│    title: String,                    // 計畫名稱                            │
│    status: "審核中"|"通過"|"未通過",  // 狀態                                │
│    members: [{                       // 組員 (最多3人)                      │
│      student_id, student_class,                                              │
│      student_seat, student_name,                                             │
│      has_submitted                                                           │
│    }],                                                                       │
│    motivation: String,               // 學習動機                            │
│    learning_categories: Object,      // 學習類別                            │
│    references: [{                    // 參考資料                            │
│      book_title, author,                                                     │
│      publisher, link                                                         │
│    }],                                                                       │
│    plan_items: [{                    // 學習計畫 (至少18小時)               │
│      date, content, hours, metric                                            │
│    }],                                                                       │
│    midterm_goal: String,             // 期中目標                            │
│    final_goal: String,               // 期末目標                            │
│    signatures: [{                    // 簽章                                │
│      type, image_url                                                         │
│    }],                                                                       │
│    comment: String,                  // 教師評語                            │
│    submitter_id: String,             // 提交者ID                            │
│    reviewer_id: String,              // 審核者ID                            │
│    created_at: DateTime,                                                     │
│    updated_at: DateTime                                                      │
│  }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  Collection: students                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│  {                                                                           │
│    _id: ObjectId,                                                           │
│    student_id: String (unique),      // 學號                                │
│    class_name: String,               // 班級                                │
│    seat_number: Number,              // 座號                                │
│    name: String,                     // 姓名                                │
│    created_at: DateTime,                                                     │
│    updated_at: DateTime                                                      │
│  }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 資料流程圖

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              主要資料流程                                    │
└─────────────────────────────────────────────────────────────────────────────┘

【登入流程】
┌──────────┐    POST /auth/login    ┌──────────┐    查詢/驗證    ┌──────────┐
│  使用者   │ ─────────────────────▶ │  Backend │ ──────────────▶ │ MongoDB  │
│ (瀏覽器)  │ ◀───────────────────── │ (FastAPI)│ ◀────────────── │  users   │
└──────────┘    JWT Token + User    └──────────┘    User資料     └──────────┘
     │
     │ 儲存Token到localStorage
     ▼
┌──────────┐
│ 導向頁面  │
│學生→Form │
│教師→History
└──────────┘

【申請提交流程】
┌──────────┐   POST /applications   ┌──────────┐     插入文件    ┌──────────┐
│  學生     │ ─────────────────────▶ │  Backend │ ──────────────▶ │ MongoDB  │
│ (填寫表單)│ ◀───────────────────── │          │ ◀────────────── │applications
└──────────┘    Application資料     └──────────┘   新建Application └──────────┘

【審核流程】
┌──────────┐  PATCH /applications   ┌──────────┐     更新文件    ┌──────────┐
│  教師     │  /{id}/review         │  Backend │ ──────────────▶ │ MongoDB  │
│(審核/評語)│ ─────────────────────▶ │          │ ◀────────────── │applications
└──────────┘ ◀───────────────────── └──────────┘   更新status    └──────────┘
                更新後的Application                 /comment

【PDF匯出流程】
┌──────────┐ GET /applications/{id} ┌──────────┐    渲染模板     ┌──────────┐
│  使用者   │    /export-pdf        │  Backend │ ──────────────▶ │  Word    │
│          │ ─────────────────────▶ │          │                 │  模板    │
└──────────┘                        │          │                 └──────────┘
     │                              │          │    LibreOffice        │
     │                              │          │ ◀────────────────────┘
     │                              │          │    轉換DOCX→PDF
     │  ◀─────────────────────────  └──────────┘
     │         PDF檔案下載
     ▼
┌──────────┐
│ 下載PDF  │
└──────────┘
```

## Docker 部署架構

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Docker Compose 架構                                │
└─────────────────────────────────────────────────────────────────────────────┘

                            docker-compose.yml
                                   │
       ┌───────────────────────────┼───────────────────────────┐
       │                           │                           │
       ▼                           ▼                           ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│    frontend     │       │     backend     │       │     mongodb     │
│   (Nginx)       │       │   (FastAPI)     │       │   (mongo:7.0)   │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ Port: 3000      │       │ Port: 8000      │       │ Port: 27017     │
│                 │       │                 │       │                 │
│ 靜態檔案服務     │       │ API服務         │       │ 資料儲存        │
│ /api → backend  │──────▶│ JWT認證         │──────▶│                 │
│                 │       │ PDF生成         │       │ Volumes:        │
│ Health Check:   │       │ LibreOffice     │       │ - mongodb_data  │
│ wget /          │       │                 │       │ - mongodb_config│
│                 │       │ Health Check:   │       │                 │
│ Depends On:     │       │ curl /health    │       │ Health Check:   │
│ - backend       │       │                 │       │ mongosh ping    │
│                 │       │ Depends On:     │       │                 │
└─────────────────┘       │ - mongodb       │       └─────────────────┘
                          └─────────────────┘

                    Network: self-learning-network (bridge)
```

## 技術棧總覽

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              技術棧 Tech Stack                               │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│    Frontend     │       │     Backend     │       │    Database     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ React 19.2      │       │ FastAPI 0.115   │       │ MongoDB 7.0     │
│ TypeScript 5.8  │       │ Python 3.11     │       │ Beanie ODM      │
│ Vite 6.2        │       │ Uvicorn         │       │ Motor (async)   │
│ Tailwind CSS    │       │ Beanie 1.26     │       │                 │
└─────────────────┘       │ JWT + Bcrypt    │       └─────────────────┘
                          │ LibreOffice     │
┌─────────────────┐       │ docxtpl         │       ┌─────────────────┐
│   UI Design     │       └─────────────────┘       │   DevOps        │
├─────────────────┤                                 ├─────────────────┤
│ Glass Morphism  │                                 │ Docker          │
│ Dark Theme      │                                 │ Docker Compose  │
│ Responsive      │                                 │ Nginx           │
│ Noto Sans TC    │                                 │ Health Checks   │
└─────────────────┘                                 └─────────────────┘
```

## 檔案結構

```
self-learn-system/
├── 📁 backend/
│   ├── 📁 app/
│   │   ├── 📁 database/
│   │   │   └── mongodb.py          # MongoDB連線管理
│   │   ├── 📁 models/
│   │   │   ├── application.py      # 申請表模型
│   │   │   ├── student.py          # 學生模型
│   │   │   └── user.py             # 使用者模型
│   │   ├── 📁 routes/
│   │   │   ├── applications.py     # 申請表API
│   │   │   ├── auth.py             # 認證API
│   │   │   └── students.py         # 學生API
│   │   ├── 📁 services/
│   │   │   ├── application_service.py
│   │   │   ├── pdf_service.py      # PDF生成
│   │   │   ├── student_service.py
│   │   │   └── user_service.py
│   │   ├── 📁 utils/
│   │   │   └── auth.py             # JWT工具
│   │   ├── config.py               # 配置
│   │   ├── dependencies.py         # 依賴注入
│   │   └── main.py                 # FastAPI入口
│   ├── 📁 scripts/
│   │   └── import_students.py      # 學生匯入腳本
│   ├── Dockerfile
│   └── requirements.txt
│
├── 📁 components/
│   ├── 📁 ui/
│   │   └── GlassUI.tsx             # Glass UI組件庫
│   ├── ApplicationDetailModal.tsx
│   ├── ApplicationFormPage.tsx
│   ├── ChangePasswordModal.tsx
│   ├── CommentModal.tsx
│   ├── Header.tsx
│   ├── HistoryPage.tsx
│   ├── LoginPage.tsx
│   └── SignaturePad.tsx
│
├── 📁 services/
│   └── api.ts                      # API服務層
│
├── App.tsx                         # React主應用
├── index.tsx                       # 入口點
├── index.html                      # HTML模板
├── types.ts                        # TypeScript型別
│
├── docker-compose.yml              # Docker編排
├── Dockerfile                      # 前端Dockerfile
├── nginx.conf                      # Nginx配置
├── vite.config.ts                  # Vite配置
├── package.json
└── tsconfig.json
```

---

## 一鍵部署

```bash
# 啟動所有服務
docker-compose up -d --build

# 匯入學生資料
docker exec self-learning-backend python scripts/import_students.py /114-1全校名單.xlsx

# 訪問系統
# Frontend: http://localhost:3000
# Backend:  http://localhost:8000
# MongoDB:  localhost:27017
```

## 預設帳號

| 角色 | 帳號 | 密碼 |
|------|------|------|
| 教師 | fhshbook@fhsh.tp.edu.tw | fhshbook |
| 學生 | {學號}@fhsh.tp.edu.tw | {學號} |
